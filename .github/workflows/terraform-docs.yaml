---
name: "terraform-docs"

on:
  pull_request:
    branches:
      - "release-**"
    paths:
      - "**.tf"

jobs:
  terraform-docs:
    uses: GersonRS/modern-gitops-stack/.github/workflows/modules-terraform-docs.yaml@main
